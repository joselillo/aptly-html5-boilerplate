# Aptly HTML5 Boilerplate

Enthält 2 Grunt Tasks: 

- `$ grunt build-all  `
  legt eine zip-Datei nach `dist/build`, die mal prinzipiell direkt als static resource nach Salesforce hochpumpen kann.
- `$ grunt build-watch`

